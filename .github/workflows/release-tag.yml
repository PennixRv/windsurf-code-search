name: Validate Release Tag

on:
  push:
    tags: ["v*.*.*"]

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "26.5.1"
      - run: npm install --global npm@12.0.1
      - run: npm ci --ignore-scripts
      - run: node scripts/release/verify-tag.mjs "${{ github.ref_name }}"
      - run: node scripts/release/verify-release-evidence.mjs
      - run: npm test
      - run: npm run verify:provenance
      - run: npm run pack:check
      - run: npm pack --dry-run --json --ignore-scripts
      - run: node scripts/release/build-package.mjs --output dist/tag-package
